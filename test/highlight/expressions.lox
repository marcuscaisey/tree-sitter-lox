// Literals
1;
// <- number
"one";
// <- string
true;
// <- boolean
nil;
// <- constant.builtin

// Identifier
foo;
// <- variable

// Unary Expressions
!1;
// <- operator
-1;
// <- operator

// Binary Expressions
1, 2;
 // <- punctuation.delimiter
1 == 2;
  // <- operator
1 != 2;
  // <- operator
1 < 2;
  // <- operator
1 <= 2;
  // <- operator
1 > 2;
  // <- operator
1 >= 2;
  // <- operator
1 + 2;
  // <- operator
1 - 2;
  // <- operator
1 * 2;
  // <- operator
1 / 2;
  // <- operator

// Ternary expression
1 ? 2 : 3;
  // <- keyword.conditional.ternary
  //  ^ keyword.conditional.ternary

// Group expression
( 1 );
// <- punctuation.bracket
//  ^ punctuation.bracket
//   ^ punctuation.delimiter
